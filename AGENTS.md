# AGENTS.md — EV 인센티브 자격확인/매칭 앱

이 문서는 Codex가 매 세션 시작 시 자동으로 읽는 아키텍처 스펙입니다.
Codex는 작업을 완료할 때마다 이 문서와 README.md를 함께 갱신합니다 (아래 0번 규칙 참고).

## 0. 작업 완료 시 필수 규칙

0. 작업을 완료하면 이 AGENTS.md의 "현재 상태" 섹션과 "다음 세션에서 할 일" 섹션을 갱신한다.
1. 사용자(Jaden)에게 새로 확인이 필요한 사항이 생기면 "확인 필요 항목" 섹션에 추가한다.
2. README.md는 사람이 읽는 문서이므로, 기능이 추가/변경되면 README.md의 기능 목록도 함께 갱신한다.
3. 기존 파일 구조를 임의로 재구성하지 않는다. 재구성이 필요하다고 판단되면 코드를 바꾸지 말고
   "제안 사항" 섹션에 이유와 함께 기록한다.
4. 개인정보(소득, 연락처 등)를 다루는 코드를 수정할 때는 Google Sheets 저장 경로와
   localStorage 저장 경로를 명확히 구분해서 유지한다 — 민감정보는 localStorage에 남기지 않는다.
5. CONFIG 객체(프로그램별 금액·소득기준)를 수정할 때는 로직 코드는 건드리지 않는다.
6. 변경 후 git commit을 남긴다. 커밋 메시지는 한국어로 무엇을/왜 바꿨는지 명시한다.

## 1. 프로젝트 목적

캘리포니아 EV 인센티브 프로그램(RYR, Clean Cars 4 All, DCAP, MyFirstEV, CALeVIP,
유틸리티 충전기 리베이트)에 대해 고객의 정보를 받아 자격을 판정하고, 어떤 프로그램에
신청 가능한지 매칭해주는 웹앱. 신청 이후에는 케이스 상태를 이력과 함께 관리한다.

## 2. 파일 구조

```
index.html          — 메인 앱 (단일 파일, 인라인 CSS/JS)
payment-success.html — Stripe 세션 검증 및 접수 결과·재시도 화면
admin.html          — 대표님(운영자) 전용 관리 화면
landing.html        — 소셜 광고 유입용 랜딩 페이지
about.html          — 서비스 소개 및 제작 취지
legal.html          — 법적고지, 개인정보처리방침, 면책조항
site-config.js      — 공개 페이지 공통 설정(문의 이메일 등)
apps-script/Code.gs — Google Apps Script 백엔드 (Apps Script 에디터에 수동 배포)
AGENTS.md            — 이 문서
README.md            — 사람이 읽는 설명서
```

## 3. index.html 요구사항

### 3.1 인테이크 폼 (다단계, 진행률 표시)
- 서비스 선택은 차량정보보다 앞에서 3개 라디오로 받되 무료 자격확인 화면에는 금액을 표시하지 않는다. 내부 가격은 vehicleOnly($99)/chargerOnly($149)/bundle($199, $49 할인)을 유지한다. chargerOnly는 기존차량 단계 전체를 숨기고 입력을 비활성화·required 해제하며, 앞뒤 이동과 전체 검증에서도 제외한다. 다른 유형으로 전환하면 필수 검증을 복원한다.
- 이름·전화·이메일·ZIP·주택소유, 가구원수·소득·신고연도, 서비스 유형·기존EV, 과거 신청여부는 필수다. 기존차 연식·연료·본인명의와 희망 구매유형은 차량만/번들에서만 필수다. 희망 차종(PHEV/ZEV/전기오토바이/전기자전거)과 전기패널 용량은 선택이다. 단계 이동 시 인라인 오류를 표시하고 최종 제출 시 전체 단계를 재검증한다. 서버도 새 결제대기 신청의 필수값과 형식을 확인한다.
- 신청자 기본정보: 이름, 전화, 이메일, 우편번호, 주택소유여부(자가/렌트)
- 소득정보: 가구원수, 가구총소득(또는 신고소득), 신고연도
- 기존차량: 연식, 연료타입, 본인명의여부, 스모그체크통과여부
- 희망사항: 신차/인증중고/충전기만, 희망차종, 기존EV보유여부
- 신청이력: 과거 CVRP/RYR/CC4A/DCAP 신청여부(있으면 프로그램명+결과)

### 3.2 우편번호 매핑
- ZIP → air district(South Coast AQMD 여부), 유틸리티 회사 매핑을 JS 객체로 하드코딩
- 정확한 전국 매핑이 아니므로 "추정치 — 확인 필요" 배지를 항상 표시

### 3.3 매칭 룰 엔진 (순수 JS 함수로 분리, 테스트 가능하게)
- 신규 결제대기 저장 시 서버도 기존 자격판정식과 ProgramStatus로 결과를 다시 계산한다. vehicleOnly와 bundle은 기존 전체 판정을 재사용하고, chargerOnly는 차량 판정 없이 유틸리티리베이트만 계산한다. 브라우저/서버 판정 설정과 결과의 일치 여부를 테스트한다. chargerOnly 관리자 화면에는 "차량 프로그램: 해당없음(신청 안 함)"을 표시한다.
- 매칭결과 구조: vehiclePrograms/chargerPrograms 그룹화 - 금액순 정렬 + 상호배타 경고, admin.html에서 순위 배지로 표시한다. RYR/CC4A/DCAP가 둘 이상 해당되면 mutuallyExclusiveWarning에 "이 중 하나만 신청 가능합니다"를 기록한다. 기존 context는 보존한다.
- 기존 eligible 함수를 재사용한다. 충전기 판정에만 purchaseType=charger인 복사본을 전달하며 원본 차량 희망사항은 변경하지 않는다. chargerOnly는 유틸리티만, bundle은 기존 충전기 판정(CALeVIP 개인고객 적용 주의 포함)을 유지한다. ProgramStatus의 비활성 배지를 유지한다.
- 새 신청은 서버에서 재계산한 그룹 결과를 PendingPayments와 Cases 매칭결과JSON에 그대로 저장하며, 기존 results 형식도 서버와 관리자에서 읽을 수 있도록 호환한다.
- FPL % 계산 함수
- 프로그램별 자격 판정 함수 (boolean + 사유 텍스트 반환)
- 최종 추천: 금액 내림차순 정렬, 상호배타 조합은 경고 배지

### 3.4 결과 화면
- 고객에게는 매칭 프로그램 카드, 예상금액, 자격사유, 필요서류를 표시하지 않는다.
- 무료 인테이크 제출 시 매칭 룰 엔진을 실행해 신청·매칭 정보를 Leads에 저장한다. 화면에는 가능성 유무만 안내하고 가격을 표시하지 않는다. 가능성 안내 HTML 이메일에서 선택 품목과 이용료를 처음 공개하되 구체적인 프로그램명·예상 지원금은 노출하지 않는다.
- 가능성 있는 고객이 이메일의 `leadId` 링크로 재진입해 동의·결제를 시작할 때만 PendingPayments에 임시 저장하고, 결제 확인 후 Cases의 `매칭결과JSON`으로 옮긴다.
- 고객 연락 선호 방법(전화/이메일/문자)을 수집하고 전화·이메일은 제출 전 필수 검증한다.

### 3.5 케이스 이력 관리 화면
- CaseID/전화번호로 검색
- 상태 변경: 대기 → 서류접수 → 제출 → 승인 → 거절/정산완료
  - 상태 변경 시 StatusHistory에 append (기존 이력 절대 덮어쓰지 않음)
- 상태별 파이프라인(칸반) 뷰

## 4. Google Sheets 데이터 구조

**Sheet 1 "Cases"**: CaseID, 생성일시, 담당자, 신청자정보(JSON), 매칭프로그램목록(JSON), 연락선호방법, 매칭결과JSON, 현재상태, 최종수정일시, 컨트랙터ID, 배정일시, 확인메일발송상태(대기/완료/실패), 결제일시, 환불처리여부, 환불처리일시, 환불사유, 연계딜러ID, 딜러소개일시, 소개비수령여부, 소개비수령액, 소개비수령일, DAC상태, LADWP제3자지정안내여부, LADWP절차단계

**Sheet 2 "StatusHistory"**: CaseID, 타임스탬프, 이전상태, 새상태, 메모, 담당자 (append-only, row 삭제/수정 금지)

**Sheet 3 "Contractors"**: 컨트랙터ID, 이름, 연락처, 액세스코드, 계약시작일, 계약서Drive링크, 라이선스사본링크, 본드사본링크, 사업자증빙링크, 건당단가, 활성여부, 생성일시, 최종수정일시

**Sheet 4 "Payments"**: CaseID, 컨트랙터ID, 하청비지급액, 하청비지급일, 하청비지급상태(대기/완료), 정부정산수령액, 정부정산수령일, 정부정산수령상태(대기/완료)

**Sheet 5 "ContractorApplications"**: 지원ID, 지원일시, 업체명/이름, 연락처, 이메일, 서비스가능지역, 라이선스번호, 라이선스종류, 라이선스만료일, 본드회사명, 본드번호, 본드보장금액, 본드만료일, 경력/한줄소개, 지원상태, 검토일시, 검토메모, 컨트랙터ID

**Sheet 6 "Admins"**: 관리자ID, 이름, 이메일, 비밀번호, 활성여부, 생성일시, 최종로그인일시. 빈 시트는 기존 `ADMIN_PASSWORD` 값으로 `ADM-001` 대표님 계정을 자동 생성한다.

**Sheet 7 "Dealers"**: 딜러ID, 딜러명, 담당자명, 연락처, 이메일, 참여프로그램, 소개비금액, 활성여부, 생성일시, 최종수정일시

**Sheet 8 "ProgramStatus"**: 프로그램명, 활성여부, 마지막업데이트일시, 메모, 다음확인예정일

**Sheet 9 "Leads"**: 리드ID, 생성일시, 이름, 연락처, 이메일, 신청자정보JSON, 매칭결과JSON, 추천가격유형, 리드상태(가능성있음/부적격/전환완료), 이메일발송여부, 최종수정일시

**Code.gs 함수**: `doPost(e)` 케이스 생성/상태갱신 분기, `doGet(e)` CaseID/전화번호 조회.
상태 갱신 시 Cases 갱신 + StatusHistory append가 항상 같이 일어나야 함.

## 4.1 admin.html 요구사항

- 관리자는 Admins 시트의 개별 관리자 ID와 비밀번호로 로그인한다. 기존 `ADMIN_PASSWORD` 마스터 비밀번호는 관리자 ID를 비운 로그인에서 항상 통하는 비상 백업 경로로 유지한다.
- 관리자 계정은 이름·이메일·비밀번호·활성 여부·최종 로그인 일시를 관리하며, 이 단계에서는 비밀번호를 평문으로 저장한다.
- **업체 관리**: 이름, 연락처, 액세스코드, 계약시작일, 계약서 Drive 링크, 건당 단가, 활성 여부를 등록·수정·비활성화하고 업체별 배정/완료/평균 처리기간을 표시한다.
- **일감 배정**: 미배정 Cases를 활성 업체에 배정하고, Cases 갱신과 함께 StatusHistory에 담당자=관리자, 새 상태=배정됨을 append한다.
- **대시보드**: 대기/배정됨/시공중/시공완료/서류제출완료/정산완료 칸반 요약, 업체별 집계, 배정 후 지정 일수 이상 변동 없는 지연 건을 표시한다.
- **정산**: 시공완료 이후 모든 상태(서류제출완료·정산완료 포함) 중 하청비지급상태가 완료가 아닌 건을 지급 대상으로 표시하며, 정부 정산 수령은 별도 필드로 독립 기록한다.
- **딜러 제휴**: 제휴 딜러 등록·수정·비활성화, MyFirstEV 케이스 소개, 미수령 소개비 처리를 제공하며 컨트랙터 정산과 독립적으로 관리한다.
- **케이스 상세**: 관리자만 매칭결과JSON의 프로그램명, 예상금액, 자격사유, 필요서류를 확인하며, "고객에게 연락함"으로 `결과안내완료` 상태를 StatusHistory에 기록할 수 있다.
- `Code.gs`는 `registerContractor`, `updateContractor`, `assignCaseToContractor`, `getUnassignedCases`, `getDashboardSummary`, `recordSubPayment`, `recordGovReimbursement` 동작을 API 분기로 제공한다.

## 4.2 공개 페이지 요구사항

- **landing.html**: 모바일 우선 광고 유입 페이지로, 무료 자격 확인 CTA(index.html), 혜택 요약, 민간 신청 지원 서비스 고지, 소개/법적고지 링크를 제공한다. 확정 지원금 광고 문구는 사용하지 않는다.
- **about.html**: 서비스의 정보 격차 해소 취지, 대상 프로그램, 민간 신청 대행·시공 연결 역할, 운영자/라이선스/연락처를 안내한다.
- **legal.html**: 비공식 관계·예비 판정·운영 조건 변경·비자문 면책, 이용료·조건부 환불정책, 개인정보 수집·Google Sheets 보관·비판매·SSN 미수집·삭제 요청 절차를 고지한다.
- 모든 공개 페이지의 문의 이메일은 `site-config.js`의 `SITE_CONFIG.contactEmail`을 사용하며, 값은 `jdlee.electric@gmail.com`으로 통일한다.

## 4.3 디자인 토큰

- 컨셉: 친환경 클린 에너지와 신뢰할 수 있는 서류 대행을 함께 전달하는, 한인 이민자 가정에 따뜻하고 안심되는 톤의 "클린EV" 디자인.
- 색상: 배경 `#F7F6F1`, 프라이머리 `#0F5C4C`, 프라이머리 호버 `#0B4438`, 액센트 `#E8A33D`, 기본 텍스트 `#1C1B18`, 보조 텍스트 `#5C5A54`만 사용한다.
- 폰트: Pretendard 웹폰트(`https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css`)를 한글·영문·숫자 전체에 사용한다.
- 이미지: `site-config.js`의 `SITE_CONFIG.images`에서 이미지 경로를 관리한다. 현재는 프로젝트 `assets/`의 생성 이미지(가정 충전 EV, 따뜻한 가족 사진)를 사용하며, 실제 사진으로 바꿀 위치에는 TODO 주석을 남긴다.
- 공개 페이지 혜택은 반복 카드/그림자 패턴 대신 프라이머리 그린 SVG 라인 아이콘과 텍스트 목록으로 구성한다.

## 4.4 공개 페이지 공통 네비게이션 구조

- 공개 페이지 4곳의 공유 메타태그는 HTML head에 정적으로 작성한다. URL은 PUBLIC_SITE_URL 기본값인 `https://tubasa22.github.io/ev-incentive-app`을 사용하며 배포 주소 변경 시 메타태그도 함께 갱신한다.
- `landing.html`, `about.html`, `legal.html`, `contractor-apply.html`은 기존 SVG·텍스트 로고와 `.header-dark` 대응을 유지하며 공통 헤더 메뉴를 제공한다.
- 메뉴는 서비스 소개, 협력업체 지원, 법적고지 및 프라이머리 그린 배경의 무료 자격 확인 버튼으로 구성한다. 현재 페이지 링크는 밑줄과 `aria-current="page"`로 표시한다.
- 모바일에서는 햄버거 버튼으로 드롭다운을 열고 닫으며 키보드 조작과 펼침 상태 안내를 지원한다. 자바스크립트가 꺼져도 링크에 접근할 수 있어야 한다.
- 랜딩 푸터에는 구분선과 함께 서비스 소개·협력업체로 지원하기·법적고지 및 개인정보처리방침 링크와 민간 서비스 안내를 명확히 배치한다.
- 본문과 신청 로직은 변경하지 않는다. `index.html`은 공통 메뉴 없이 기존 로고로 랜딩 페이지에 복귀하는 구조를 유지한다.

## 5. 지켜야 할 것

- FinCRM(tubasa22.github.io/Fincrm) 등 다른 저장소 파일은 건드리지 않는다 — 완전히 독립된 저장소.
- 소득 등 민감정보는 Google Sheets에만, localStorage는 세션 임시값만.
- 프로그램 기준(금액/소득기준)은 상단 CONFIG 객체로 분리.
- 관리자 전용 단가·계약정보·Payments는 admin API 응답에서만 제공하며 컨트랙터 API에는 포함하지 않는다.
- 고객/컨트랙터/관리자 앱의 기존 기능 로직은 공개 페이지 추가 시 변경하지 않으며, 푸터 링크와 공통 문의처 설정만 최소 반영한다.
- 고객용 index.html에는 상세 매칭 결과를 절대 표시하지 않고, 저장된 결과는 관리자 전용 화면에서만 검토한다.

## 5.1 Apps Script 보안 규칙

- Cases의 부분 검색과 관리자 상세 결과 조회는 `adminPassword` 인증을 통과한 관리자만 허용한다. 고객 자가 조회 기능은 현재 제공하지 않는다.
- 상태 변경은 관리자 인증 또는 해당 케이스에 실제 배정된 활성 컨트랙터의 ID·액세스코드 인증을 통과해야 한다.
- 컨트랙터 조회는 `getCasesForContractor`로만 제공하며, 소득·가구원수·신청이력·매칭결과 등 민감 필드를 제외한 축소 객체만 반환한다.
- 모든 Sheets 쓰기 함수는 `LockService` Script Lock을 획득하고 `finally`에서 항상 해제한다.

## 5.2 컨트랙터 계약·신원확인 규칙

- 컨트랙터 신원확인은 SMS OTP(자동, `FEATURES_SMS_ENABLED=true`일 때), 이메일 OTP, 또는 관리자 수동 전화 확인으로 운영한다. `SMS인증`·`이메일인증`·`관리자수동확인완료` 3가지 모두 배정 가능 상태로 인정한다. CSLB는 관리자용 조회 링크로 참고한다.
- OTP는 Contractors 시트에 5분 한정으로 저장하고 검증 성공 시 즉시 초기화한다. SMS 비활성 상태에서는 관리자 수동 전화 확인 대기로 계약서를 받을 수 있다.
- `FEATURES_SMS_ENABLED` Script Property가 정확히 `true`일 때만 Twilio를 호출하며, 누락 또는 `false`면 OTP 단계를 건너뛰고 오류 없이 관리자 수동확인 대기 흐름으로 진행한다.
- 계약서 제출은 라이선스·본드·전자서명·동의를 기록한다. SMS 사용 시 OTP 통과 후 10분 내 제출만 허용한다.
- 계약서 제출 시 라이선스 사본·본드 사본·사업자 증빙을 선택적으로 받아 `DRIVE_FOLDER_ID`가 가리키는 Google Drive 폴더에 저장하고, 파일 링크를 Contractors 시트에 기록한다. 10MB 초과 파일이나 개별 업로드 실패는 다른 파일 및 계약 저장을 막지 않는다.
- 계약서 제출 완료 후 제출 서류 여부와 개별 업로드 오류를 포함한 관리자 알림 이메일을 발송하며, 이메일 실패는 계약 저장 성공에 영향을 주지 않는다.
- 배정에는 활성·미만료 라이선스/본드·본인확인 완료 상태가 모두 필요하다.

## 5.3 프론트-백엔드 연결 규칙

- 모든 앱 화면은 `site-config.js`의 `SITE_CONFIG.apiUrl`을 공통으로 사용해 Apps Script `doPost` 액션을 호출한다. 배포 URL이 설정되기 전에는 호출하지 않고 관리자 서버 설정 안내를 표시한다.
- 네트워크 실패는 "서버에 연결할 수 없습니다. 인터넷 연결을 확인하거나 잠시 후 다시 시도해주세요"로, 서버 실패는 응답의 한국어 `error` 메시지로 표시한다.
- `file://` 직접 열기는 CORS 제약으로 실패할 수 있으므로 GitHub Pages 등 웹서버 배포 상태에서 연결을 검증한다.

## 5.4 협력업체 공개 지원·검토 규칙

- `contractor-apply.html`은 로그인 없는 공개 지원서이며 `submitContractorApplication`만 인증 없이 호출한다.
- 지원서는 동일 이메일 또는 전화번호의 24시간 내 중복 접수를 막고, 관리자는 지원 상태별 목록을 검토해 승인/거절한다.
- 승인 시 기존 `registerContractor`를 재사용해 계약 미체결 업체를 생성하고, 액세스코드와 계약서 링크를 이메일로 전달한다. 메일 실패는 승인 자체를 취소하지 않는다.
- 모든 UI 텍스트는 한국어.

## 5.5 협력업체 입력 검증 규칙

- 공개 지원서와 계약서의 연락처는 숫자와 하이픈만 허용하고, 이메일은 HTML5 및 정규식으로 다시 검증한다.
- 라이선스·본드 번호는 공백을 제거하고 최소 3자 이상인지 확인하며, 본드 보장금액은 0보다 커야 한다.
- 라이선스·본드 만료일에는 오늘부터 10년 후까지의 입력 범위를 제시한다. 과거 날짜는 제출을 막지 않되 갱신 정보 입력 안내를 표시한다.

## 5.6 프로그램 운영 상태 규칙

- `ProgramStatus` 시트는 RYR, CC4A, DCAP, MyFirstEV, CALeVIP, 유틸리티리베이트, SCAQMD_충전기리베이트의 활성 여부·메모·다음확인예정일을 관리한다. SCAQMD 항목은 기본 비활성이고 나머지는 활성으로 초기화한다.
- RYR 기본 다음 확인 예정일은 2026-11-15이며 CARB 회계연도 자금계획을 7월과 11월에 재확인하도록 메모한다. 예정일이 지난 항목은 관리자 화면에 확인 필요로 표시한다.
- 공개 `getProgramStatuses` 조회 결과를 고객용 매칭 계산에 반영한다. 비활성 프로그램도 자격 결과에는 남기되 `현재 신청 일시중단` 상태로 구분해 관리자 검토에 전달한다.
- 활성 여부 변경은 관리자만 할 수 있으며 관리자 프로그램 관리 탭에서 즉시 저장한다.

## 5.7 고객 안내 처리기간 문구

- 클린EV의 1차 검토 기간(영업일 기준 1~2일)과 정부·유틸리티 운영기관의 정식 심사 기간(프로그램별 수 주~수개월)을 구분해 안내한다.
- 랜딩 페이지의 기간 문구는 `CONFIG.reviewTimelineDays`, `CONFIG.programReviewNote`로 분리하며, 기관 심사 기간을 임의로 단축할 수 없음을 명시한다.

## 5.8 신청 대행 동의·가격 정책

- 안내 문구는 3단계 가격과 일치시킨다. 모든 유형에 3영업일 이내 취소 요청+제출 전 전액 환불 조건을 동일 적용한다. 시공비 차감은 chargerOnly($149)/bundle($199)에만 적용하고 vehicleOnly($99)는 제외한다. 법적 문구의 변호사 검토 TODO를 유지한다.
- 동의서 비용 조항은 선택한 서비스의 한글명·CONFIG.pricing 금액으로 동적 표시하고 차량 전용일 때 차감 문단을 숨긴다. 이메일·관리자·결제완료 화면은 현재 가격을 추정하지 않고 저장된 결제 유형·실제 금액을 표시한다. 기록이 없으면 확인 필요로 표시한다.
- verifySession 완료 응답에는 검증된 결제 요약(유형·한글명·금액)만 추가하며 신청자 개인정보는 노출하지 않는다. 가격 결정·서버 검증·환불 처리 로직은 이번 문구 정합성 작업에서 변경하지 않는다.

- 지원금 한도를 초과하는 충전기·전기패널 공사비는 고객 부담이며, 공사 전 견적·지원금 한도·차액을 안내하고 공사 여부를 별도로 결정한다.
- Cases에 `예상공사비`, `예상지원금한도`, `고객부담예상액` 컬럼을 추가한다. 관리자 케이스 검토에서 입력하며 고객부담액은 `max(0, 예상공사비 - 예상지원금한도)`로 서버에서 계산한다. 고객에게는 관리자가 전화/이메일로 안내한다.

- `site-config.js`의 `SITE_CONFIG.pricing`에 vehicleOnly:99, chargerOnly:149, bundle:199와 시공비 차감 안내를 관리한다. Stripe 링크는 `stripe.test`와 `stripe.live` 아래에 paymentLinkVehicleOnly/paymentLinkChargerOnly/paymentLinkBundle 세 가지로 구분하고, `testMode`로 사용할 묶음을 선택한다.
- 고객 신청 제출 전 신청 대행 동의서 전문을 끝까지 확인하고 체크한 뒤 전자서명 이름을 입력해야 하며, 동의 여부·동의 일시·전자서명을 신청자정보(JSON)에 저장한다.
- 환불정책 확정: 3일+제출전=전액환불, 그 외 환불불가 (시공비 차감은 별도 유지). 고객 고지는 결제 후 3영업일 이내 취소 요청 및 정식 신청서 제출 전 조건이다. 영업일 계산은 달력일로 근사함(자동판별은 72시간 이내). 실제 카드 환불은 Stripe 대시보드 수동 처리한다.
- Cases에 결제일시·환불처리여부·환불처리일시·환불사유를 추가한다. 결제일시는 Stripe 검증 또는 관리자 결제 확인으로 케이스 생성 시 서버가 기록한 접수 시각이다. 재시도해도 최초 시각을 유지하며 기존 데이터는 임의 소급하지 않는다.
- 관리자 환불 자동판별은 대기·배정됨·방문예정·시공중·서류접수 상태와 72시간을 함께 확인한다. 제출 이력이 있으면 상태를 되돌려도 불가 처리한다. 결제일시 누락·미래 시각 등은 수동 확인이 필요하다.
- processRefund는 관리자 인증·사유·카드 환불 완료 확인을 요구한다. 불가 건은 명시적인 예외 확인 후 [예외처리] 접두사를 붙인다. 실제 카드 환불 API는 호출하지 않고 환불 필드 및 append-only StatusHistory에 환불처리 이벤트만 기록하며 현재 업무 상태는 유지한다.
- 자동판별은 처리 시점 기준 참고 표시다. 주말·공휴일, 취소 요청일, 지연 결제 확인으로 인한 차이는 관리자가 실제 결제 내역과 대조하여 예외 처리한다. 환불 정보는 관리자 조회에만 제공하며 컨트랙터 응답에는 노출하지 않는다.
- 신청 동의 후 PendingPayments에만 임시 저장하고 Stripe Payment Link로 이동한다. 서비스 유형으로 $99/$149/$199를 결정하고 서버 고정 가격으로 재검증한다. 기존 withCharger 결제대기 건의 $199 검증은 호환하되 신규 생성에는 허용하지 않는다.
- 웹훅과 중계 서버는 사용하지 않는다. Apps Script가 Script Properties의 STRIPE_SECRET_KEY로 Stripe 세션을 직접 조회해 paid·토큰·USD 금액·일회성 결제·테스트/운영 모드를 확인한 후에만 기존 createCase_로 접수한다. 공개 createCase 요청은 차단한다.
- 성공 URL은 payment-success.html?session_id={CHECKOUT_SESSION_ID}를 사용한다. 임의 토큰 치환은 지원을 가정하지 않으며 서버가 세션의 client_reference_id로 토큰을 복원한다. token이 함께 전달되면 반드시 일치해야 한다.
- PendingPayments 컬럼: 결제토큰, 생성일시, 가격유형, 금액, 결제상태, Stripe세션ID, 신청자정보임시JSON, 매칭결과임시JSON, 최종수정일시, 테스트모드. 복구·감사를 위해 접수CaseID, 결제확인방식, 확인관리자ID, 확인관리자명, 확인일시도 저장한다.
- `verifyStripeSession`의 정확한 $99/$149/$199 금액 검증은 `testMode=false`일 때만 적용한다. 테스트 금액 검증 생략은 PendingPayments의 테스트모드가 `예`이고 Stripe 테스트 비밀키·테스트 세션이 모두 일치하는 경우로 제한하며, 결제 완료 상태 검증은 항상 유지한다.
- 결제 확정은 Script Lock으로 직렬화하고 신청자정보의 서버 지정 결제토큰으로 부분 실패 후 재호출도 중복 생성하지 않도록 복구한다. 완료 상태는 케이스와 최초 이력 저장 후 마지막에 기록한다.
- 24시간 이상 대기 건은 관리자에게만 노출한다. 수동 확인은 관리자 인증과 Stripe 세션ID·대시보드 대조 확인을 요구하며 담당자와 확인 방식을 기록한다.
- 결제 링크 미설정 시 안내만 표시하고 임시 신청도 저장하지 않는다. 비밀키 미설정·검증 실패 시 실제 케이스를 만들지 않는다. 소득 등 신청정보는 브라우저 저장소에 저장하지 않는다.
- 확인메일은 기존 processPendingConfirmationEmails 시간 기반 트리거로 발송한다. 접수 완료와 메일 발송 완료를 혼동하지 않는다.

## 5.9 CC4A/DCAP 매칭 기준

- `CC4A_RULES` 데이터로 정확한 공식 금액을 반영한다(2024-25 Funding Plan 기준). FPL 300% 이하, 차량 유형별 일반/DAC 금액, 충전 지원·개조장비 한도, 평생 1회 및 폐차 원칙을 관리자 검토 데이터에 유지한다.
- DAC 판정과 관할 air district 자동판정은 아직 지원하지 않는다. 일반 지역 금액으로 보수적으로 계산하고 `admin.html`에서 CalEnviroScreen 및 관할 지역 수동 확인 메모를 안내한다. 추후 CalEnviroScreen API 연동을 고려할 수 있다.
- 폐차 차량이 없으면 완전 제외하지 않고 DCAP 금융지원 대안을 조건부 후보로 안내한다. 희망 차종이 없으면 ZEV 기준으로 계산하되 차종별 금액 차이를 명시한다.

## 5.10 딜러 리드 소개·소개비 관리

- 딜러 리드소개 기능을 관리자 전용으로 제공한다. `Dealers` 시트로 제휴딜러를 관리하고, MyFirstEV 매칭 케이스에 한해 딜러 소개 및 소개비 수령을 독립적으로 추적한다.
- 대표님은 고객 확인 후 제휴 딜러에게 리드를 소개하는 역할로 한정하며 가격 협상이나 거래 조건에 관여하지 않는다. 순수 소개 역할로 한정해 오토브로커 면허 불필요 구조를 유지한다.
- 딜러 소개·소개비 정보는 고객 및 컨트랙터 화면에 노출하지 않는다. 소개비는 하청비 지급·정부정산 수령과 별개의 관리자 정산 항목으로 기록한다.

## 5.11 무료 자격확인·유료 정식신청 분리

- 기본 `index.html`은 결제·동의 없이 무료 자격확인 정보만 접수한다. 기존 매칭엔진, 차량 서비스 운영 토글, ProgramStatus를 재사용해 서버에서 판정하고 `Leads` 시트에 저장한다.
- 활성 프로그램 가능성이 있으면 `가능성있음`, 없으면 `부적격`으로 기록한다. 자격확인 화면은 품목만 선택하고 금액을 표시하지 않는다. 가능성 안내 이메일에서 추천 품목명·이용료·3단계 가격을 처음 공개하되 구체적 프로그램명과 예상 지원금은 계속 비공개로 유지한다.
- 가능성있음·부적격 리드 이메일은 로고, 브랜드 색상, 카드 레이아웃, plain text 폴백을 갖춘 HTML 형식으로 발송한다.
- `가능성있음` 이메일의 고유 `leadId` 링크로 재진입한 경우에만 저장된 신청자정보를 서버에서 불러온다. 추천 서비스 유형·이용료는 수정 불가능한 확인 정보로 표시하고, 유형을 바꾸려면 기본 자격확인을 처음부터 다시 진행하도록 안내한 뒤 신청 대행 동의·Stripe 결제를 진행한다. 민감정보는 브라우저 저장소에 보관하지 않는다.
- `leadId` 재진입은 서버 응답 전 기본 자격확인 폼을 노출하지 않고 로딩 화면만 표시한다. 공개 리드 조회는 불필요한 전체 시트 초기화를 피하고 Leads 시트의 리드ID 열만 조회한다.
- 결제 확인 후 기존 `createCase_`로 정식 Cases를 만들고 해당 리드를 `전환완료`로 갱신한다. 무료 관심등록 `submitVehicleInterest` 흐름은 별도로 유지한다.
- 관리자는 리드상태별 목록을 확인하고, 가능성있음 상태로 7일 이상 결제 전환되지 않은 리드의 안내 이메일을 재발송할 수 있다.

## 5.12 서비스 지역·주력 프로그램 우선순위

- 서비스 지역은 LA·오렌지카운티·사우스베이의 South Coast AQMD 관할을 중심으로 한다.
- 주력 프로그램은 RYR/CC4A/MyFirstEV/유틸리티 리베이트(SCE·LADWP 중심)다. DCAP은 South Coast AQMD 관할 밖 대체용으로 격하하고, 관할 판정이 불명확할 때만 수동 확인 메모와 함께 후보로 유지한다.
- CALeVIP은 개인고객 해당성이 낮은 보조 프로그램으로 격하해 충전기 프로그램 배열과 소개 화면의 마지막에 배치한다. 기존 자격판정·CC4A_RULES 금액·딜러소개·3단계 가격 로직은 변경하지 않는다.

## 5.13 유틸리티·SCAQMD 기준과 DAC 수동 확인

- `UTILITY_RULES`는 SCE Charge Ready Home($4,200 소득기준/$2,100 DAC), SCE 중고 EV 리베이트($1,000/$4,000), LADWP 충전기($1,000+저소득 $500+전용미터 $250), LADWP 중고 EV 리베이트($1,500/$4,000)를 관리한다.
- LADWP 충전기 리베이트는 제3자 지급 지정이 가능하다. SCE Charge Ready Home과 달리 별도 계약업체 등록 없이 신청서에서 대표님 사업체를 지급대상으로 바로 지정할 수 있으며, 이 옵션을 활용해 고객 실부담을 낮출 수 있다. 관리자는 고객 안내 여부를 Cases에 기록한다.
- LADWP 관할 충전기 케이스는 기존 업무 상태와 별도로 8단계 행정절차 체크리스트를 `contractor.html`에서 제공한다. 온라인 신청서 제출(4단계)은 전기허가 취득·시공(6단계)보다 반드시 먼저 완료해 지연을 방지하도록 강조한다. 다세대주택에는 전기 설계도면으로 기간이 늘어날 수 있다는 안내를 각 단계에 표시한다.
- South Coast AQMD 주거용 EV 충전 리베이트는 최대 $500, 저소득 증빙·면허 전기기사·허가·3년 유지·선착순 기준으로 관리하며 운영 확인 전 기본 비활성으로 둔다.
- DCAP Financing Assistance는 2026-08-21 기준 신규 신청 중단 상태로 안내한다. 폐차 차량이 없는 경우 `확인 필요(현재 대안 경로 제한적)`으로 표시한다.
- DAC는 외부 API 없이 관리자가 ArcGIS SB 535 지도와 신청자 주소/ZIP을 대조해 `DAC 해당`/`DAC 미해당`/`미확인`으로 기록한다. CC4A와 SCE Charge Ready Home은 이 판정에 따라 일반·DAC 금액을 표시한다.

## 6. 현재 상태

- 화면 구조:

| 구분 | 파일 | 역할 |
| --- | --- | --- |
| 공개 | landing.html | 소셜 광고 유입 및 무료 자격 확인 유도 |
| 공개 | about.html | 서비스 취지·프로그램·운영자 정보 안내 |
| 공개 | legal.html | 면책, 이용료·조건부 환불정책, 개인정보처리방침 |
| 공개 | contractor-apply.html | 로그인 없이 접수하는 협력업체 지원서 |
| 공개 | index.html | 고객 인테이크 및 인센티브 예비 매칭 |
| 앱 | contractor.html | 컨트랙터 배정 케이스·계약 관리 화면 |
| 앱 | admin.html | 운영자 업체·배정·파이프라인·정산 관리 |
| 공통 | site-config.js | 공개 페이지 문의처 설정 |

### 완료된 것 (검증 완료)

- PendingPayments에 테스트모드를 기록하고, Stripe 테스트 키·테스트 세션과 일치하는 테스트 결제는 소액이어도 완료 상태를 검증해 접수한다. 운영 결제는 기존 서비스 정가 검증을 그대로 적용한다.
- `leadId` 링크 진입 시 기본 폼이 잠깐 보이는 현상을 제거하고 즉시 로딩 스피너를 표시한다. 성공 시 저장 서비스 확인·동의 화면, 실패 시 기존 링크 오류 안내로 전환하며, 서버 조회도 Leads 단일 시트·리드ID 열 검색으로 경량화했다.
- Stripe 테스트 Payment Link 3개를 `SITE_CONFIG.stripe.test`에 연결했으며 `testMode: true` 상태에서는 테스트 링크만 사용한다. 운영 링크는 `stripe.live` 플레이스홀더로 분리했다.
- 리드 이메일의 `leadId` 링크로 재진입하면 저장된 추천 서비스 유형과 이용료를 읽기 전용으로 확인하고 바로 동의서로 이어진다. 재진입 화면에서는 유형을 다시 선택할 수 없으며 변경 희망 시 무료 자격확인을 처음부터 다시 진행하도록 안내한다.
- 무료 자격확인 화면은 차량/충전기/둘 다 품목만 선택하도록 가격 표기를 제거했고, 추천 품목·이용료는 가능성 안내 이메일에서 처음 공개한다. 가능성있음·부적격 리드 이메일 모두 브랜드 HTML 형식과 plain text 폴백을 사용한다.
- 자격확인(무료)과 결제(유료)를 `Leads` 시트로 분리했다. 자격확인 후 가능성 유무를 이메일로 안내하고, 가능성 있는 고객은 고유 링크로 저장 정보를 재사용해 동의·결제를 진행한다. 이메일에는 서비스 이용료만 공개하고 구체적 프로그램명·예상 지원금은 노출하지 않으며, 결제 확인 후에만 Cases가 생성되고 리드가 전환완료된다. `admin.html`에서 상태별 리드 관리와 7일 이상 미전환 리드 안내 재발송을 지원한다.
- LADWP 관할 충전기 케이스에 8단계 행정절차 체크리스트를 추가했다. 컨트랙터 본인에게 배정된 건만 순서대로 완료할 수 있고, 진행도·append-only 이력을 저장하며 관리자 상세에 요약한다. 다세대주택 참고와 4단계 선행 경고를 포함한다.
- LADWP 충전기 리베이트의 제3자 지급 지정 규칙·신청 동의 안내·고객 실부담 절감 안내를 반영하고, 관리자 케이스 상세에서 상담 안내 여부를 기록하도록 구현했다.
- ProgramStatus에 다음 확인 예정일과 기한 경과 배지를 추가하고 RYR 기본 점검 일정·메모를 반영했다. SCE/LADWP 유틸리티 기준, SCAQMD 주거용 충전 리베이트(기본 비활성), DCAP Financing Assistance 중단 안내, 관리자 DAC 지도·주소 복사·수동 판정과 금액 표시를 구현했다.
- 서비스 지역을 LA/OC/사우스베이(South Coast AQMD 관할) 중심으로 정리했다. 주력 프로그램은 RYR/CC4A/MyFirstEV/유틸리티리베이트(SCE·LADWP 중심)이며, DCAP은 관할 밖 대체용으로 격하하고 CALeVIP은 개인고객 비해당 보조 프로그램으로 마지막에 배치했다.
- 딜러 리드소개 기능 추가: Dealers 시트로 제휴딜러 관리, MyFirstEV 케이스에 한해 딜러 소개 및 소개비 수령 추적 가능. 대표님은 가격협상/거래조건에 관여하지 않는 순수 소개 역할로 한정함(오토브로커 면허 불필요 구조 유지).
- CC4A/DCAP 매칭을 2024-25 Funding Plan 기준의 `CC4A_RULES`로 정교화했다. FPL 300%·차종별 일반 지역 금액·폐차 차량 미보유 조건부 DCAP 대안·충전 지원을 구조화하고, DAC/관할 air district는 관리자 수동 확인 메모로 표시한다.
- 3단계 가격의 법적고지·동의서·접수확인 이메일·관리자 상세·결제완료 문구를 정합화했다. 동의서는 선택 유형/금액을 동적으로 표시하고 vehicleOnly의 시공비 차감 문구를 숨긴다. 이메일·관리자·완료 화면은 저장된 실제 결제 유형/금액만 표시한다.
- 가격 3단계: vehicleOnly($99)/chargerOnly($149)/bundle($199, $49 할인). 서비스 라디오·차량 단계 숨김/필수 복원·서버 가격 및 매칭 재계산·3개 Stripe 플레이스홀더·관리자 충전기 전용 표시·법적고지 가격을 반영했다. 모의 테스트 통과, 실결제/브라우저 배포 확인은 별도 필요하다.

- 신청 필수 필드의 required·인라인 오류·최종 전체 단계 검증과 서버 재검증을 보강하고 전기패널 용량 선택 입력을 추가했다. 매칭결과 구조: vehiclePrograms/chargerPrograms 그룹화 - 금액순 정렬 + 상호배타 경고, admin.html에서 순위 배지로 표시. 기존 eligible·ProgramStatus·고객 결과 비노출을 유지하고 구형 results 및 신규 그룹 결과의 결제 후 저장을 모의 테스트로 검증했다.
- about.html 프로그램 아코디언을 차량 구매 지원(RYR, CC4A/DCAP, MyFirstEV)과 충전기·전기공사 지원(CALeVIP, 유틸리티) 두 그룹으로 구분했다. 개별 확인 안내를 섹션 최상단에 배치하고 기존 프로그램 설명·아코디언 스타일·스크립트·운영자 정보는 그대로 보존했다.
- 환불정책 확정: 3일+제출전=전액환불, 그 외 환불불가 (시공비 차감은 별도 유지). 영업일 계산은 달력일로 근사함. 실제 카드 환불은 Stripe 대시보드 수동 처리. legal/index 문구, Cases 결제일시·환불 필드, 관리자 가능/불가 배지·예외 처리·중복 방지·이력 복구를 구현하고 모의 테스트로 검증했다. 실제 Apps Script 배포 및 화면 확인은 필요하다.
- 신청 동의 → 가격 분기 → 결제대기 → Stripe API 직접 검증 → 자동 접수·메일 대기 흐름과 결제 성공/재시도 페이지를 구현했다. 24시간 이상 미확인 건의 관리자 수동 접수·감사 기록을 추가했다. 서버 및 화면 스크립트 모의 테스트 통과, 실제 Stripe 결제·배포 검증은 미실시이며 링크와 비밀키는 미설정 상태다.
- Open Graph 메타태그 적용됨, og:image 파일: `assets/clean-ev-hero.png`. 공개 페이지 4곳에 페이지별 제목·설명·URL과 Twitter 대형 이미지 카드 메타태그를 추가했다.
- 공개 페이지 4곳에 공통 네비게이션·무료 자격 확인 버튼·모바일 햄버거 메뉴·현재 페이지 표시를 추가하고 랜딩 푸터를 강화했다. 본문·기존 스크립트 보존, 링크 대상, 메뉴 이벤트를 자동 검증했다. 실제 브라우저 화면 확인은 후속 확인 항목이다.
- 지원금 한도 초과 공사비는 고객 부담 원칙 반영, Cases 시트에 `예상공사비`/`예상지원금한도`/`고객부담예상액` 필드 추가. 관리자 케이스 검토에서 기록하며 서버에서 고객부담액을 계산한다.

- 신청 대행 동의서와 가격체계(`CONFIG.pricing`) 반영 (3영업일 이내 취소 요청+제출 전 전액 환불·시공비 차감 정책). 결제 흐름 구현 완료, 실제 Stripe 계정·링크·비밀키 설정 및 운영 검증 필요
- 관리자 다중 계정 도입: Admins 시트 기반 개별 ID 로그인·계정 관리·최종 로그인 기록을 지원하고, 기존 `ADMIN_PASSWORD` 마스터 비밀번호는 비상 백업 경로로 유지
- 고객 신청서(`index.html`)와 컨트랙터 지원~계약 전체 파이프라인: `contractor-apply.html` → 관리자 승인 → 이메일 초대 → OTP/이메일/관리자 수동 본인확인 → `contract.html` 라이선스·본드 정보 및 Drive 서류 업로드 → `contractor.html` 포털
- 관리자 패널(`admin.html`): 업체 관리, 일감 배정, 프로그램 온·오프 토글, 정산 관리(하청비 지급/정부 정산 수령 독립 추적)
- 케이스 전체 라이프사이클 실제 테스트 완료: 고객 제출 → 관리자 배정 → 컨트랙터 상태 변경(배정됨~시공완료) → 관리자 하청비 지급 처리
- 지원 접수·승인·계약 완료 각 단계별 지원자·관리자 알림 이메일
- 공개 페이지(`landing.html`, `about.html`, `legal.html`) 로고·헤더 디자인 통일
- 연락처 이메일 확정: `jdlee.electric@gmail.com`

### 아직 안 된 것 (다음 세션 우선순위)

1. 관리자 비밀번호 해싱 검토 (현재 Admins 시트에 평문 저장)
2. `landing.html`에 UTM 광고 유입 추적 파라미터 캡처
3. Stripe 테스트 계정·Payment Link·비밀키 설정, Apps Script 새 버전 배포 후 실제 결제/메일/관리자 수동 확인 검증 (별도 eligibility-check/consent-payment 페이지 대신 기존 index.html 동의 단계 재사용)
4. `about.html`에 라이선스번호(1059763, C-10, CSLB) 반영 여부 최종 화면 확인 필요

## 7. 다음 세션에서 할 일

- 최신 Apps Script 재배포 후 가능성있음/부적격 HTML 리드 이메일의 로고·가격·CTA·줄바꿈을 Gmail 모바일/데스크톱에서 실제 확인한다.
- Apps Script 새 버전 배포 후 무료 자격확인 → Leads 저장·가능성/부적격 이메일 → `leadId` 링크 재진입 → 동의·결제 → 전환완료·Cases 생성 흐름과 관리자 재발송을 실제 환경에서 확인한다.
- 3개 Stripe Payment Link($99/$149/$199)를 설정하고 Code.gs 새 버전 배포 후 각 유형의 신청·결제·접수를 실제 환경에서 확인한다.

- Code.gs 새 버전 배포 후 필수 항목 누락·인라인 오류·관리자 그룹/순위/비활성 표시를 실제 브라우저와 Sheets에서 확인한다.
- about.html 프로그램 그룹 구분의 모바일·데스크톱 표시를 최종 확인한다.
- Code.gs 새 버전 배포 후 관리자 환불 배지·예외 확인·완료 표시를 실제 화면에서 확인하고, Stripe 대시보드 수동 환불 기록 절차를 검증한다.
- 배포 후 카카오톡·페이스북 등에서 실제 공유 미리보기를 확인한다.
- 공개 페이지 공통 메뉴를 실제 모바일·데스크톱 브라우저에서 최종 화면 확인한다.
- 견적 금액 고객 자동 이메일 안내 검토 (현재는 관리자 수동 안내)
- Apps Script 재배포 후 3가지 가격 유형별 접수확인 이메일·관리자 상세·결제완료 화면의 유형/금액 표시를 실제 환경에서 확인한다.
- CC4A/DCAP의 DAC·관할 air district 자동판정을 위한 CalEnviroScreen 및 공식 지역 데이터 연동 가능성을 검토한다.
- 실제 운영 전 딜러 소개 계약서와 소개비 지급 조건, 순수 소개 범위에 대한 법률·세무 검토를 진행한다.
- 실제 서비스 ZIP 표본으로 South Coast AQMD 안/밖/불명확 지역의 DCAP 후순위 처리와 SCE·LADWP 안내를 배포 화면에서 확인한다.
- Apps Script 재배포 후 ProgramStatus 다음 확인 예정일 마이그레이션, SCAQMD 기본 비활성, DAC 판정 저장 및 실제 Sheets 열 추가를 확인한다.
- LADWP 관할 충전기·번들 신청에서 고객 안내문과 관리자 제3자 지급 안내 체크가 실제 Sheets에 저장되는지 확인한다.
- Apps Script 재배포 후 LADWP 체크리스트의 컨트랙터 배정 검증, 1~8단계 순차 저장, StatusHistory 기록과 다세대주택 안내를 실제 포털에서 확인한다.

1. 관리자 비밀번호 해싱 검토
2. `landing.html`에 UTM 광고 유입 추적 파라미터 캡처
3. Stripe 결제 흐름 실제 테스트 및 운영 설정: 정상·취소·중복·브라우저 종료·수동 확인·확인메일 트리거 검증
4. `about.html`의 라이선스번호(1059763, C-10, CSLB) 최종 화면 확인

## 8. 확인 필요 항목

- Leads에는 결제 전 신청자의 소득·차량·연락처 정보가 저장된다. 부적격 및 장기 미전환 리드의 개인정보 보관기간과 삭제 기준을 운영 전에 확정해야 한다.
- SCE POEV 지급 방식은 운영 전 공식 최신 안내를 다시 확인한다. 현재 규칙 데이터는 소개비나 컨트랙터 직접수령이 아닌 고객 귀속으로 기록한다.
- SCAQMD 충전 리베이트는 2026-09 기준 신청 가능 여부가 미확정이므로 기본 비활성 상태에서 포털을 직접 확인한 뒤 활성화한다.
- 가격체계 변경에 따른 법적고지/동의서/이메일 문구 전수 정합성 점검 완료, 변호사 검토는 여전히 필요.
- 결제 링크 키는 `stripe.test`/`stripe.live` 각각의 paymentLinkVehicleOnly/paymentLinkChargerOnly/paymentLinkBundle이다. 테스트 링크 3개는 연결되어 있고 `testMode: true`이며, 운영 링크는 아직 플레이스홀더다. 운영 전 운영 링크·운영 비밀키를 함께 설정하고 `testMode: false`로 전환해야 한다. 브라우저와 Code.gs의 MATCH_CONFIG(프로그램 기준) 변경 시 양쪽 설정을 동기화하고 일치 테스트를 실행한다.

- 현재 about.html 원문에는 RYR 충전기 보조금 및 유틸리티 패널 업그레이드 문구가 없다. 이번 그룹 재배치는 설명 수정 금지 요청에 따라 현재 설명을 그대로 보존했으며, 사용자가 언급한 별도 정정본과의 차이는 후속 확인이 필요하다.
- 환불 자동판별은 현재 시각 기준 72시간 근사이며 고객 고지의 3영업일·취소 요청일과 다를 수 있다. 주말·공휴일, 지연 결제 확인, 결과안내완료/시공완료 등 명시적 허용 목록 밖의 상태는 실제 제출 여부를 관리자가 대조해야 한다. 결제일시는 실제 Stripe 청구시각이 아니라 서버 결제 확인·접수 시각이므로 이 값만으로 환불을 최종 거절하지 않는다. 기존 결제일시 미기록 케이스는 자동판별 불가로 표시한다.
- Stripe 계정 생성 후 할 일: (1) 일회성 USD Payment Link 3개 생성($99/$149/$199) 후 site-config.js의 CONFIG.stripe(SITE_CONFIG.stripe)에 URL 입력, 성공시 이동 URL은 `https://tubasa22.github.io/ev-incentive-app/payment-success.html?session_id={CHECKOUT_SESSION_ID}`로 설정, (2) Stripe 대시보드의 비밀키(Secret key)를 Script Properties의 STRIPE_SECRET_KEY에 저장 (공개용 Publishable key 아님, 주의).
- 토큰 전달 방법 확인 완료: 임의 `{토큰}` 치환은 가정하지 않는다. 결제 링크에 client_reference_id를 추가하고 성공 페이지에는 Stripe가 치환한 세션ID만 전달한다. 서버가 Stripe API 응답의 client_reference_id로 신청 토큰을 복원한다. URL에 token이 있으면 일치 여부도 검증한다. 웹훅·중계 서버·STRIPE_WEBHOOK_SECRET은 사용하지 않는다.
- Stripe Payment Link는 수량 1 고정, USD, 할인·자동 세금·통화 변환 없이 설정해야 서버의 $99/$149/$199 검증을 통과한다. 테스트 키는 별도 테스트 시트에서 사용하고 운영 시 실제 결제 링크와 운영 비밀키로 함께 전환한다.
- 변경된 Code.gs는 Apps Script에 수동 반영하고 새 버전으로 배포해야 한다. 기존 1분 주기 processPendingConfirmationEmails 트리거를 유지한다. 브라우저 종료 시 자동 검증이 실행되지 않으므로 관리자가 24시간 이상 대기 목록을 정기 확인해야 한다.
- PendingPayments에도 소득·연락처·동의 정보가 저장된다. 시트 접근권한과 장기 미결제 개인정보 보관·삭제 기한을 운영 전에 확정해야 한다.
- og:image 비율이 권장 사이즈와 다름 - 전용 썸네일 이미지 제작 검토 (현재 1448×1086, 4:3 / 권장 1200×630, 약 1.91:1). 이번 작업에서는 기존 이미지를 유지했다.

- 프로그램별 실제 금액·소득기준과 지역별 대상 여부는 공식 공고 기준으로 검증·갱신 필요
- Apps Script Script Properties에 ADMIN_PASSWORD를 설정해야 관리자 로그인이 작동함
- 보안 수정 완료, 배포 전 재점검 필요
- CSLB는 공식 API가 없어 수동확인 방식 채택함, 추후 물량이 많아지면 유료 스크래핑 서비스(Apify 등) 검토 가능
- FEATURES_SMS_ENABLED Script Property를 Google Sheets 편집기에서 `true`로 추가하고 진짜 Twilio 값을 넣으면 SMS 기능 활성화됨
- DRIVE_FOLDER_ID Script Property가 자동 생성되며, 처음 실행 시 Drive 접근 권한 팝업이 뜰 수 있음(테스트 함수를 편집기에서 한 번 직접 실행해 권한 승인 필요 — 이메일 권한 승인했던 것과 동일한 방식)
- 신청 대행 동의서와 환불정책은 실제 법적 효력이 필요한 문서이므로 변호사 검토 필요 (특히 캘리포니아 Home Solicitation Sale 관련 법규, Civil Code 1689.5 검토). 이번 작업은 요청받은 운영 정책의 구현이며 법적 적합성 확정이 아니다.
